//
//  GrapeTest.swift
//  knowhow
//
//  Created by F1rec on 2025/7/25.
//

import SwiftUI
// import Grape  // 暂时屏蔽Grape

/*
struct GrapeTestView: View {
    var body: some View {
        VStack {
            Text("Grape独立测试")
                .font(.title)
                .padding()
            
            Graph {
                NodeMark(id: "node1") {
                    Circle()
                        .fill(.blue)
                        .frame(width: 60, height: 60)
                    
                    Text("Test")
                        .foregroundColor(.white)
                }
            }
            .frame(height: 300)
            .background(Color.gray.opacity(0.1))
            .cornerRadius(12)
            .padding()
        }
    }
}

#Preview {
    GrapeTestView()
}
*/ 
